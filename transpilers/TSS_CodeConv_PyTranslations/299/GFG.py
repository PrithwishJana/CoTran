class GFG:
    @staticmethod
#JAVA TO PYTHON CONVERTER TASK: Python does not allow method overloads:
    def printRoots(a, b, c):
        print(str(1) + ", " + str(c / (a * 1.0)))
    @staticmethod
#JAVA TO PYTHON CONVERTER TASK: Python does not allow method overloads:
    def main(args):
        a = 2
        b = 3
        c = - 5
        GFG.printRoots(a, b, c)
