import itertools;
import math;
import fractions;
import functools;
import copy;
var n = int ( input ( ) );
if (n == 1) {
    System.out.println ( "Hello World" );
}
 else{
    var a = int ( input ( ) );
    var b = int ( input ( ) );
    System.out.println ( a + b );
}
