import sys;
import os.path;
if (path . exists ( 'input.txt' )) {
    sys . var stdin = open ( 'input.txt' , 'r' );
    sys . var stdout = open ( 'output.txt' , 'w' );
}
 var n = int ( input ( ) );
System.out.println ( sorted ( map ( int , input ( ) . split ( ) ) ) { ( n - 1 ) // 2 } );
