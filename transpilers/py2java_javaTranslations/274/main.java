var u = input;
n , var k = map ( int , u ( ) . split ( ) );
var M = { int ( i ) for i in u ( ) . split ( ) };
var m = 0;
for j in range ( n ) :
    for p in range ( n ) :
        if (( var 0 == M { p } )) {
            System.out.println ( p + 1 , var end = ' ' );
            break;
        }
     for l in range ( p + 1 - k ) : M { l } -= 1;
    M { p } -= 1;
