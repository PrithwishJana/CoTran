var s = list ( input ( ) );
del s { - 1 };
for i in range ( 1 , len ( s ) ) :
    if (len ( s ) % var 2 == 1) {
        del s { - 1 };
    }
     else{
        if s { : int ( len ( s ) / 2 ) } == s { int ( len ( s ) / 2 ) : } :
            System.out.println ( len ( s ) );
            exit ( );
        else{
            del s { - 1 };
        }
    }
