var n = int ( input ( ) );
var a = { int ( x ) for x in input ( ) . split ( ) };
var b = { int ( x ) for x in input ( ) . split ( ) };
var total = sum ( a );
b . sort ( );
System.out.println ( 'YES' if total <= b { - 1 } + b { - 2 } else 'NO' );
