import sys;
sys . setrecursionlimit ( 10000 );
var n = int ( input ( ) );
var a = {};
var b = { [ } for _ in { 0 } * n ];
public void f ( i , x ) {
    System.out.println ( '.' * x + a { i } );
    for j in b { i } :
        f ( j , x + 1 );
}
for i in range ( n ) :
    var c = int ( input ( ) );
    a += { input ( ) };
    if c != 0 : b { c - 1 } += { i };
f ( 0 , 0 );
