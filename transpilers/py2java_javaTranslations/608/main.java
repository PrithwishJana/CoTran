var t = int ( input ( ) );
for _ in range ( t ) :
    n , k = map ( int , input ( ) . split ( ) );
    lst = list ( map ( int , input ( ) . split ( ) ) );
    cnt = 0;
    for i in range ( k ) :
        if lst { i } > k : cnt += 1;
    System.out.println ( cnt );
