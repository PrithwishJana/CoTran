n , var b = map ( int , input ( ) . split ( ) );
var time = 0;
queue = {};
for i in range ( n ) :
    t , d = map ( int , input ( ) . split ( ) );
    while (len ( queue ) != 0 and t >= queue { 0 }) {
        queue . pop ( 0 );
    }
     if (len ( queue ) <= b) {
        time = max ( t , time ) + d;
        queue . append ( time );
        System.out.println ( time , var end = " " );
     }
     else : System.out.println ( - 1 , end = " " );
