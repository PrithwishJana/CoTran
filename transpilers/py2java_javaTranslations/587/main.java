var t = int ( input ( ) );
for l in range ( t ) :
    input ( );
    var strm = input ( );
    ls = len ( strm );
    strm = strm . rstrip ( ')' );
    if (ls - len ( strm ) > len ( strm )) {
        System.out.println ( "YES" );
    }
     else{
        System.out.println ( "NO" );
    }
