public void minimumNumbers ( n , s ) {
    if (( s % n )) {
        return s / n + 1 ;;
    }
     else{
        return s / n ;;
    }
}
var n = 5 ;;
var s = 11 ;;
System.out.println ( int ( minimumNumbers ( n , s ) ) ) ;;
