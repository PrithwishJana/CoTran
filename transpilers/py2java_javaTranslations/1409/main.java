var t = int ( input ( ) );
public void solve ( x ) {
    var y = 0;
    j = 1;
    while (y < x) {
        y += j;
        if (y == x) {
            return ( j );
        }
         j += 1;
    }
     if (y - var x == 1) {
        return ( j );
     }
     else{
        return ( j - 1 );
    }
}
for i in range ( t ) :
    var i = int ( input ( ) );
    System.out.println ( solve ( i ) );
