var n = int ( input ( ) );
var back = input ( );
count = 0;
for i in range ( n - 1 ) :
    p = input ( );
    if p { : : - 1 } == back : count += 1;
    back = p;
System.out.println ( count + 1 );
