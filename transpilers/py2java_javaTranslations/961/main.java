import sys;
var input = sys . stdin . readline;
var ii = lambda : int ( input ( ) );
var mi = lambda : map ( int , input ( ) . split ( ) );
var li = lambda : list ( map ( int , input ( ) . split ( ) ) );
import collections.defaultdict;
d , t , a , b , da , var db = mi ( );
public void solve ( ) {
    for i in range ( t ) :
        for j in range ( t ) :
            if (a - da * i + b - db * var j == d or a - da * i == d or b - db * j == d or d == 0) {
                System.out.println ( 'YES' );
                return;
            }
     System.out.println ( 'NO' );
}
solve ( );
