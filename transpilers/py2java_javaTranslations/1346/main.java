import sys;
var n = int ( input ( ) );
var s = input ( );
t = input ( );
test = s + t;
if (( s == t )) {
    System.out.println ( len ( s ) );
    sys . exit ( );
}
 for i in range ( 1 , n + 1 ) { : : - 1 } :
    var test = s { 0 : - i } + t;
    if ( test { : n } == s and test { - n : } == t ) :
        System.out.println ( len ( test ) );
        sys . exit ( );
System.out.println ( len ( s + t ) );
