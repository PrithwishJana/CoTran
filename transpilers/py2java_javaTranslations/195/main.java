_ , var a = map ( int , input ( ) . split ( ) );
var days = list ( map ( int , input ( ) . split ( ) ) );
var names = 0;
var lists = 0;
for (int i = 0; i < days.length; i++){
    names += i;
    System.out.println ( names // a - lists , var end = ' ' );
    lists += names // a - lists;
}
